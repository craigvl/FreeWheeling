@model FreeWheeling.UI.Models.AllRideComments
@using FreeWheeling.Domain.Entities

@{
    ViewBag.Title = "See All Comments";
}

@if (@Model.PreviousID != -1)
{
    @Html.ActionLink("<-- Back to Bunch", "Index", "Ride", new { RideId = @Model.PreviousID, Groupid = @Model.GroupId }, new { @class = "btn btn-default", onclick = "ShowProgress();" })
}
else
{
    @Html.ActionLink("<-- Back to Bunch", "Index", "Ride", new { RideId = @Model.RideId, Groupid = @Model.GroupId }, new { @class = "btn btn-default", onclick = "ShowProgress();" })
}


<h2>All Comments</h2>

@foreach (Comment item in @Model.Comments)
{
  @item.userName @Html.Raw(":")  @item.CommentText
    <p></p>
}

<div class="loading" style="display:none;" align="center">
    Loading. Please wait.<br />
    <br />
    <img src="~/Content/Images/loader.gif" alt="" />
</div>
@section scripts{
<script>
    jQuery(document).ready(function () {
        HideProgress();
    });
</script>
}
