@model FreeWheeling.UI.Models.HomeIndexModel
@{
    ViewBag.Title = "Home Page";
}

@if (Model.LocationsId != 0)
{
  <span class="btn btn-primary"> @Model.CurrentUserLocation </span>  @Html.ActionLink(" ", "LocationChange", "Home", new { }, new { @class = "glyphicon glyphicon-pencil" }) 
}
else
{

    using (Html.BeginForm())
    {
        @Html.DropDownListFor(
             x => x.LocationsId,
             new SelectList(
             Model.Locations.ToList().Select(
             x => new { Value = x.id, Text = x.Name }
            ),
            "Value",
            "Text", Model.LocationsId
            ), "-- Set a Location --", new {@class = "form-control" }
            )
        <p></p>
        <input type="submit" value="Set Location" class="btn btn-primary" />
    }    
}
    <p></p>
    <hr />
    <p></p>
    @if (Model.LocationsId != 0)
        {
            <p class="text-left">@Html.ActionLink("Groups", "Index", "Group", new { }, new { @class = "btn btn-primary" }) @Html.ActionLink("+", "Create", "Group", new { }, new { @class = "btn btn-success" }) </p>
            <p class="text-left">@Html.ActionLink("Favourite Groups", "MyGroups", "Group", new { }, new { @class = "btn btn-primary" })</p>
            <p class="text-left">@Html.ActionLink("Ad Hoc Rides (" + @Model.UpCommingAd_HocCount + ")", "AddHocList", "Ride", new { }, new { @class = "btn btn-primary" }) @Html.ActionLink("+", "CreateAdHoc", "Group", new { }, new { @class = "btn btn-success" }) </p>

            <hr />     
        }